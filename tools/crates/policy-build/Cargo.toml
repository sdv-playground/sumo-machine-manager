[package]
name = "policy-build"
version = "0.1.0"
edition = "2021"
description = "Build read-only policy images (squashfs for Linux, qnx6 for QNX guests). AUTH-ARCH-001 §4."

[dependencies]
policy-partition = { path = "../../../crates/policy-partition" }

[dev-dependencies]
tempfile = "3"

[[bin]]
name = "policy-build"
path = "src/main.rs"
