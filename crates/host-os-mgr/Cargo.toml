[package]
name = "host-os-mgr"
version = "0.1.0"
edition = "2021"
description = "Host OS update manager — A/B boot partition, IFS activation, reboot coordination"

[dependencies]
# The activators ARE the crate: `machine_mgr::BankActivator` + a log line.
# nv-store/sha2/async-trait/serde_json went out with `HostOsComponent` — the
# dead `Component` impl no production wiring ever constructed.
machine-mgr = { path = "../machine-mgr" }
tracing = "0.1"
