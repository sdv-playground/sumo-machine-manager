[package]
name = "policy-partition"
version = "0.1.0"
edition = "2021"
description = "Policy directory loader (AUTH-ARCH-001 §4 — delivered in-bank via existing OTA)"

[dependencies]
policy-eval = { path = "../policy-eval" }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"

[dev-dependencies]
tempfile = "3"
